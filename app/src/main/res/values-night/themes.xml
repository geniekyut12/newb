<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CVERDETOTOO" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:buttonTint">@color/Green</item>
        <item name="android:textColor">@color/LBrown</item>
    </style>
</resources>