<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_capture">

        <!-- Header TextView -->
        <TextView
            android:id="@+id/tvHeader"
            android:layout_width="0dp"
            android:layout_height="150dp"
            android:background="@drawable/header"
            android:gravity="center"
            android:layout_marginBottom="30dp"
            android:text="@string/capture"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Character and Chat Bubble in a LinearLayout -->
        <LinearLayout
            android:id="@+id/charac1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="50dp"
            android:padding="6dp"
            app:layout_constraintTop_toBottomOf="@id/tvHeader"
            app:layout_constraintStart_toStartOf="parent">

            <ImageView
                android:id="@+id/imageViewCharacter"
                android:layout_width="150dp"
                android:layout_height="200dp"
                android:layout_margin="6dp"
                android:src="@drawable/main_character"
                android:contentDescription="@string/pic" />

            <!-- Chat bubble TextView for messages -->
            <TextView
                android:id="@+id/textViewChatBubble"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="8dp"
                android:background="@drawable/chat_bubble"
                android:padding="8dp"
                android:textColor="#000000"
                android:textSize="16sp"
                android:maxWidth="300dp"
                android:gravity="center"
                android:visibility="gone" />
        </LinearLayout>

        <!-- Prediction Results -->
        <TextView
            android:id="@+id/textViewResult"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            style="@style/blurr"
            android:textColor="@color/WRed"
            android:fontFamily="@font/poppinsregular"
            app:layout_constraintTop_toBottomOf="@id/imageViewInput"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Score Display -->
        <TextView
            android:id="@+id/textViewScore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/score"
            android:layout_marginEnd="15sp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/WRed"
            android:padding="16dp"
            app:layout_constraintTop_toBottomOf="@id/textViewResult"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- ImageView for the captured/uploaded image -->
        <ImageView
            android:id="@+id/imageViewInput"
            android:layout_width="0dp"
            android:layout_height="250dp"
            android:layout_marginTop="-70dp"
            android:scaleType="centerCrop"
            android:background="@drawable/imgviewres"
            android:contentDescription="@string/pic"
            app:layout_constraintTop_toBottomOf="@id/charac1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Buttons for Taking Photo and Uploading Image -->
        <LinearLayout
            android:id="@+id/btnimage1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="30sp"
            android:orientation="horizontal"
            android:gravity="center_horizontal"
            app:layout_constraintTop_toBottomOf="@id/textViewScore"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <Button
                android:id="@+id/buttonTakePhoto"
                android:layout_width="wrap_content"
                android:layout_height="60dp"
                android:layout_marginBottom="44dp"
                android:background="@drawable/captbtn"
                android:text="@string/captureimg"
                android:textColor="@color/BlckRed"
                android:fontFamily="@font/poppinsmedium"
                app:backgroundTint="@null" />

            <Button
                android:id="@+id/buttonUploadImage"
                android:layout_width="wrap_content"
                android:layout_height="60dp"
                android:layout_marginStart="32dp"
                android:layout_marginBottom="44dp"
                android:background="@drawable/uploadbtn"
                android:text="@string/upload"
                android:textColor="@color/BlckRed"
                android:fontFamily="@font/poppinsmedium"
                app:backgroundTint="@null" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
